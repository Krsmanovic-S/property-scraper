# Core HTTP + parsing
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.1.0          # faster BS4 parser; fall back to html.parser if absent

# JavaScript-rendering fallback (optional but recommended)
playwright>=1.43.0
